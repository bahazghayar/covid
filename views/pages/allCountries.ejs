<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="style.css">

</head>
<body>
    <main id="country"> 

        <a href="./">Home</a>
        <a href="./allCountries">All Countris</a>
        <a href="./myRecords">My Records</a>

        <div id="card">
           <% data.forEach((item)=>{  %>
               <p>Country: <%= item.country %>  </p>
               <p>total Confirmed: <%= item.totalconfirmed %>  </p>
               <p>total Deaths: <%= item.totaldeaths %>  </p>
               <p>total Recovered: <%= item.totalrecovered %>  </p>
               <p>Date: <%= item.date %>  </p>
               
            <form action="/myRecords" method="POST">
                <input type="hidden" name="country" value="<%= item.country %>">
                <input type="hidden" name="totalconfirmed" value="<%= item.totalconfirmed %> ">
                <input type="hidden" name="totaldeaths" value="<%= item.totaldeaths %>">
                <input type="hidden" name="totalrecovered" value="<%= item.totalrecovered %>">
                <input type="hidden" name="date" value="<%= item.date %>">
                              
                <input type="submit"  value="Add to my records">
            </form>
         <%  }) %> 
        </div>

    </main>
</body>
</html>